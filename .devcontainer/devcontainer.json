{
  "name": "My Codespace",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y wget && wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb && sudo dpkg -i cloudflared-linux-amd64.deb"
}
